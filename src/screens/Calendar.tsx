// Calendar (Increment 2 §2.4). Stub so the landing index resolves in Phase 4;
// the grid, lists, and export modal land in Phase 6.

import { Bulletin, Masthead } from '../components/Bulletin';

export default function Calendar() {
  return (
    <Bulletin>
      <Masthead
        title="Calendar"
        sub="Your reps, your commitments, and what's open to you."
      />
    </Bulletin>
  );
}

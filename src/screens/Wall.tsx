// The Wall (spec §7.9). Warm content inside the retained ops shell. Phase 8.
export default function Wall() {
  return (
    <div data-surface="warm" className="-mx-8 -my-8 min-h-[70vh] bg-canvas px-8 py-12 text-primary">
      <h1 className="font-display text-5xl font-bold uppercase tracking-[0.08em]">The Wall</h1>
    </div>
  );
}
